name: View Folder Contents

on:
  push:
    branches:
      - main

jobs:
  view_folder_contents:
    runs-on: ubuntu-latest

    steps:
      - name: View Contents of Folder
        run: ls -l /home/runner/work/testDevOps2
