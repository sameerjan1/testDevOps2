name: Unity Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    

     steps:
      - name: Echo Project Path
        run: echo "Project path: ${{ github.workspace }}"
    
            
            

     

             #mfmfmfmff
