name: Echo Project Path

on:
  push:
    branches:
      - main

jobs:
  echo_project_path:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Project Path
        run: ls ${{ github.workspace }}

